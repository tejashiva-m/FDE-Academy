clouds = ("Azure", "AWS", "GCP")

print(clouds)
print(clouds[0])
print(len(clouds))

clouds[0] = "Oracle"
# Hint: Think about data that should never be modified, like months of the year or geographic coordinates.